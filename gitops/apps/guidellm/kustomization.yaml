apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - results-pvc.yaml
  # Benchmark run recipes (suspended CronJobs, triggered on demand via
  # `kubectl create job --from=cronjob/...`). Add a sibling recipe per config.
  - cronjob-qwen05b-fp16-sync.yaml
